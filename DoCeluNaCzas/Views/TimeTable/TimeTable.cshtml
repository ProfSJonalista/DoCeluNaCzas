@model DCNC.Bussiness.Models.MinuteTimeTable

<body>
    <!--<div class="delays-header"style="text-align:center;">
        <a id="btnChange" class="btnDelays collapse-header collapsed" data-toggle="collapse" href="#collapseExample" role="button" aria-expanded="false" aria-controls="collapseExample" onclick="GetDelays(@ViewBag.stopId);">
            <strong>Opóźnienia </strong><i class="fas fa-caret-down"></i>
        </a>
    </div>-->

    <div class="routes-table-container delays-table-container">

        <!--<table id="delaysTable" class="table">
            <thead>
                <tr>
                    <th class="col-md-2">Numer linii</th>
                    <th class="col-md-7">Kierunek</th>
                    <th class="col-md-3">Odjazd</th>
                </tr>
            </thead>



        </table>-->

        <div id="res"></div>
    </div>
    <div id="Timetable" class="tabcontent">

        @foreach (var day in Model.MinuteDictionary)
        {
        <div class="week-name">
            @if (day.Key.ToString() == "Weekday")
            {
            <text>
                Dni powszednie
            </text>
            }

            @if (day.Key.ToString() == "Saturday")
            {
            <text>
                Soboty
            </text>
            }

            @if (day.Key.ToString() == "Sunday")
            {
            <text>
                Niedziele i święta
            </text>
            }


        </div>
        foreach (var list in day.Value)
        {
        <text>
            <table class="timetable-table" id="timetableTable">
                <tr class="row-content">
                    <td class="hours">
                        @list.Key
                    </td>

                    @foreach (var minute in list.Value)
                    {
                    <td class="minutes">
                        @minute
                    </td>
                    }
                </tr>
            </table>
        </text>
        }
        }


    </div>



    <!--Script references. -->
    <!--Reference the jQuery library. -->
    <!--<script src="/Scripts/jquery-1.6.4.min.js" "></script>-->
    <!--Reference the SignalR library. -->
    <!--<!--<script src="/Scripts/jquery.signalR-2.4.1.min.js"></script>-->
    <!--Reference the autogenerated SignalR hub script. -->
    <!--<script src="~/signalr/hubs"></script>-->
    <script>
        $(document).ready(() => {
            Connect(@ViewBag.stopId);
                    //GetDelays();
                });
    </script>

</body>